import LocalizedStrings from 'react-native-localization';
import en from './en.json';

const InitiateInwardRemittanceLabels = new LocalizedStrings({
  en,
});

export default InitiateInwardRemittanceLabels;
